using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tapeGenarate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GetComponent<>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
